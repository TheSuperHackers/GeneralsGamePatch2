
MiscAudio
  RadarNotifyUnitUnderAttackSound = RadarNotifyUnitUnderAttack      ; played when the radar indicates a unit is under attack
  RadarNotifyHarvesterUnderAttackSound = RadarNotifyHarvesterUnderAttack ; played when the radar indicates a harvester is under attack
  RadarNotifyStructureUnderAttackSound = RadarNotifyStructureUnderAttack ; played when the radar indicates a structure is under attack
  RadarNotifyUnderAttackSound = RadarNotifyUnderAttack          ; played when the radar indicates something is under attack
  RadarNotifyInfiltrationSound = NoSound                         ; played when the radar indicates something is infiltrated
  RadarNotifyOnlineSound = RadarOn                         ; played when the radar goes online
  RadarNotifyOfflineSound = RadarOff                        ; played when the radar goes offline
  LockonTickSound = LockonTick                      ; played when a Stealth Fighter is doing lock-on
  DefectorTimerTickSound = DefectorTimerTick               ; played when a unit is defecting, etc.
  AllCheerSound = UI_AllCheerSound                ; played when the units cheer
  BattleCrySound = BattleCrySound                  ; played for battle cry
  GUIClickSound = GUIClick                        ; played when the user presses a button in UI
  NoCanDoSound = NoCanDoSound                    ; played when the user can't perform an action
  StealthDiscoveredSound = StealthDiscoveredSound          ; I just discovered a stealthed enemy unit
  StealthNeutralizedSound = StealthNeutralizedSound         ; My stealthed unit was just discovered by the enemy
  MoneyDepositSound = MoneyDeposited                  ; Money has been deposited in my account
  MoneyWithdrawSound = MoneyWithdraw                   ; Money has been withdrawn from my account
  BuildingDisabled = BuildingDisabled                ; Building has lost power, been hit with an EMP, or disable-hacked.
  BuildingReenabled = BuildingEnabled                 ; Building has recovered from being disabled.
  VehicleDisabled = BuildingDisabled                ; Vehicle has been disabled via EMP or Hacker attack.
  VehicleReenabled = BuildingEnabled                 ; Vehicle has recovered from being disabled.
  SplatterVehiclePilotsBrain = VehicleSnipeVictim              ; Pilot has been sniped by Jarmen Kell.
  TerroristInCarMoveVoice = CarBombVoiceMove                ; Terrorist in car has been ordered to move.
  TerroristInCarAttackVoice = CarBombVoiceAttack              ; Terrorist in car has been ordered to attack.
  TerroristInCarSelectVoice = CarBombVoiceSelect              ; Terrorist in car has been selected.
  CrateHeal = CrateHeal                       ; When a unit picks up a heal crate.
  CrateShroud = CrateShroud                     ; When a unit picks up a shroud crate.
  CrateSalvage = SalvageUpgrade                  ; When a unit picks up a salvage crate.
  CrateFreeUnit = CrateFreeUnit                   ; When a unit picks up a free unit crate.
  CrateMoney = CrateMoney                      ; When a unit picks up a money crate.
  UnitPromoted = UnitPromoted                    ; When a unit gets promoted
  RepairSparks = RepairSparks                    ; When a Battle Drone repairs a unit.
  SabotageShutDownBuilding = SabotageBuildingPower ; temp                   ; When a Saboteur hits a building he shuts down
  SabotageResetTimeBuilding = SabotageBuilding ; temp           ; When a Saboteur hits a building he resets the timer on
  AircraftWheelScreech = JetSkid                         ; Plays whenever aircraft land.
End
